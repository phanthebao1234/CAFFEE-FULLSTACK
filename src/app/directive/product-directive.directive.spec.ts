import { ProductDirectiveDirective } from './product-directive.directive';

describe('ProductDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
